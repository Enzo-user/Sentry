version: "3.9"
services:
  web:
    image: nginx:latest
    privileged: true
    ports:
      - "0.0.0.0:80:80"
    cap_add:
      - SYS_ADMIN
    network_mode: host
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: supersecret123
      POSTGRES_USER: admin
    pid: host
